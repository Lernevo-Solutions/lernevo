server {
    listen ${PORT};  # Cloud Run PORT variable-ஐ use பண்ணும்

    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        try_files $uri /index.html;
    }

    # Optional: cache for assets
    location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|svg)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public";
    }

    # Optional: disable .htaccess or hidden files
    location ~ /\. {
        deny all;
    }
}